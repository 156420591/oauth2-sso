spring:
  main:
    allow-bean-definition-overriding: true
  output:
    ansi:
      enabled: ALWAYS #控制台彩色输出
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&useSSL=false&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true
#    username: root
#    password: guoph
#    type: com.zaxxer.hikari.HikariDataSource
#    sql-script-encoding: UTF-8
#
#    hikari:
#      ##  Hikari 连接池配置 ------ 详细配置请访问：https://github.com/brettwooldridge/HikariCP
#      ## 最小空闲连接数量
#      minimum-idle: 5
#      ## 空闲连接存活最大时间，默认600000（10分钟）
#      idle-timeout: 180000
#      ## 连接池最大连接数，默认是10
#      maximum-pool-size: 10
#      ## 此属性控制从池返回的连接的默认自动提交行为,默认值：true
#      auto-commit: true
#      ## 连接池名称
#      pool-name: hikariDbPool
#      ## 此属性控制池中连接的最长生命周期，值0表示无限生命周期，默认1800000即30分钟
#      max-lifetime: 1800000
#      ## 数据库连接超时时间,默认30秒，即30000
#      connection-timeout: 30000
#      connection-test-query: SELECT 1

#    jpa:
#      hibernate:
#        ddl-auto: update #ddl-auto:设为update表示每次都不会重新建表,设为create表示每次都重新建表
#      show-sql: true
#      #    database-platform: org.hibernate.dialect.MariaDBDialect
#      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
#      database: mysql

    #  # Redis服务器地址
    #  redis:
    #    port: 6379
    #    host: 127.0.0.1
    #    # Redis服务器连接密码（默认为空）
    ##    database: 1
    ##    password:
    #    # 连接池最大连接数（使用负值表示没有限制）
    #    lettuce:
    #      pool:
    #        max-active: 100
    #        # 连接池最大阻塞等待时间（使用负值表示没有限制）
    #        max-wait: 6000
    #        # 连接池中的最大空闲连接
    #        max-idle: 100
    #        # 连接池中的最小空闲连接
    #        min-idle: 10
    #    # 连接超时时间（毫秒）
    #    timeout: 2000
    # redis.cluster
#    cluster:
#      nodes: 172.17.0.3:6379
#      max-redirects: 8
#    sentinel:
#      nodes:


logging:
  level:
    org:
#      springframework: TRACE
      springframework: INFO
#      springframework: DEBUG
    com:
      alibaba:
        nacos: ERROR